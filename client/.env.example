# Development: point at the Express API (optional if using Vite proxy with relative /api URLs)
VITE_API_URL=http://localhost:5000

# Production build served by Express: leave VITE_API_URL unset for same-origin requests.
